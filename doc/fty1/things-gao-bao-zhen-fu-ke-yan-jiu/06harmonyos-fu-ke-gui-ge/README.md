# 06｜HarmonyOS 复刻规格

实现顺序已经冻结为：

**H00 Visual Acceptance Harness → H01 Foundation Tokens → H02 TodoCheckbox → H03 TodoRow → H04 TodoMetadata → H05 ExpandedTodo → H06 Checklist → Organization → Overlay/Pickers → Interaction Components → Pages**。

平台遵循 HarmonyOS 的 vp/fp、安全区、触达区域、键盘与可访问性基础能力；视觉目标仍以 Things 当前 iPhone Light Mode 真机证据为准。

每个 H 任务只有在 Fixture、Geometry、Screenshot、Interaction 和 Regression 通过后才可从 IMPLEMENTED 升级为 VERIFIED。
