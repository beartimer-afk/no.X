# 04.3｜Checklist

ChecklistItem 属于 Todo，但不是完整 Todo。它拥有自己的完成状态和 Todo 内局部排序。

禁止为了代码复用直接把 TodoRow 缩小后当 ChecklistRow：两者的产品层级、交互和 metadata 能力不同。

Checklist 的排序仅在当前 Todo 范围有效，不应复用全局 ContextOrder 的 Project/Today context。

后续 H06 需要独立研究：Add item、完成态、reorder、键盘连续新增、删除、空状态以及 expanded Todo 中的高度联动。
